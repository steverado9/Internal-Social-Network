import { Request, Response } from "express";
import pool from "../db/db.config";

export default class GifCommentController {
    //create on comment
    async createComment(req: Request, res: Response): Promise<void> {
        
    }
}